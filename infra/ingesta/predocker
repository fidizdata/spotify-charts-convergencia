
#postgres DB
docker run -it --rm -e POSTGRES_USER="admin" -e POSTGRES_PASSWORD="admin" -e POSTGRES_DB="spotify_charts" -v spotify_charts_postgres_data:/var/lib/postgresql -p 5432:5432 postgres:18-bookworm

#consutlar BD desde terminar
uv run pgcli -h localhost -p 5432 -u admin -d spotify_charts


#pgadmin
docker run -it -e PGADMIN_DEFAULT_EMAIL="admin@admin.com" -e PGADMIN_DEFAULT_PASSWORD="admin" -v pgadmin_data:/var/lib/pgadmin  -p 8085:80 dpage/pgadmin4
#link
http://localhost:8085/browser/

#crear la imagen con el dockerfile
docker build -t mi_imagen_personalizada .

#correr el contenedor
docker run -it --rm --network=infra_default --env-file .env -v "${PWD}/../../data:/data" spotify_charts_ingest:latest


#docker-compose
docker-compose up -d

docker-compose down